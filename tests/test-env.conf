# === On-Prem 模拟 VPC ===
ONPREM_VPC_CIDR="172.16.0.0/16"
ONPREM_SUBNET_CIDR="172.16.1.0/24"       # 单一公有子网 (CGW + 流量发生器共用)
CGW_INSTANCE_TYPE="t4g.small"
TRAFFIC_GEN_INSTANCE_TYPE="t4g.medium"
TRAFFIC_GEN_COUNT="5"                      # 5 台发生器模拟多源访问

# === 监控 VPC 业务主机 ===
BIZ_HOST_INSTANCE_TYPE="t4g.medium"
BIZ_HOST_COUNT="3"                         # 3 台业务主机

# === VPN ===
VPN_STATIC_ROUTES="10.0.0.0/16"

# === 测试参数 ===
TRAFFIC_DURATION="120"                     # 流量持续时间(秒)
